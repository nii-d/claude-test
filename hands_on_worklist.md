# 「ミニOTAハンズオン演習」本番セッション 作業洗い出し・所要時間見積もり

対象マイコン: Renesas RX65N系（CK-RX65N / Renesas Starter Kit+ for RX65N-2MB を想定）

---

## 0. まず訂正しておきたい前提

企画段階では「異常系のフェイルオーバーを実現するには、公開鍵の検証ロジックを自分たちで実装する必要があるはず」という想定だったが、調査の結果これは誤り。

RX65N向けにAWSとRenesasが公式に提供している参照実装（[AWS公式チュートリアル](https://docs.aws.amazon.com/freertos/latest/userguide/download-rx65n-ota.html)、現行の [`renesas/iot-reference-rx`](https://github.com/renesas/iot-reference-rx)）には、以下がすべて**ミドルウェアとして実装済み**で提供されている。

| 機能 | 実装状況 | 必要な作業 |
|---|---|---|
| 署名検証（ECDSA/PKCS#11） | 提供済み | `code_signer_public_key.h` に公開鍵を貼り付けるだけ（コード実装は不要、設定のみ） |
| デュアルバンク切り替え | 提供済み | `BSP_CFG_CODE_FLASH_BANK_MODE` 等の設定のみ |
| 起動失敗時の自動ロールバック | 提供済み | `BL_ENABLE_ROLLBACK=1` を設定するだけ |

つまり「正常系」「異常系（フェイルオーバー）」「署名検証」はいずれも**実装ではなく設定・確認作業**であり、暗号ロジックやブートローダーを自作する必要はない。

唯一の"実装"らしい作業は、**異常系デモ用にわざと壊れたファームウェア**（例: 起動直後に無限ループへ入るだけの改変）を用意すること。これも数行〜数十行程度の改変で済む。

この事実により、当初懸念していた「独自実装にかかる工数」は当日のセッション時間にはほぼ乗らない。時間がかかるとしたら「AWSコンソール操作」「マイコンの書き込み・Wi-Fi設定」「新卒が初めて触る操作でのつまずき」の方である。

---

## 1. 2つの実施シナリオ

事前にどこまでインストラクター側が準備しておくかで、当日の所要時間が大きく変わる。

- **シナリオA（事前準備済み）**: Thing／証明書／ポリシー／S3バケット／AWS Signerプロファイル／初期ファームウェア書き込み済みボードを、インストラクターが事前に用意しておく。新卒は「OTAジョブを作って流す」体験に集中する。
- **シナリオB（ゼロから）**: 新卒自身がAWS IoT Coreの設定（Thing作成・証明書発行・ポリシーアタッチ）やボードへの初回書き込みから行う。AWSコンソール操作やマイコン書き込みに不慣れなため、つまずきによる遅延を見込む必要がある。

以下の章立てでは、両シナリオの所要時間を併記する。

---

## 2. 章立てと手順（手順書レベル）

### 第0章: オリエンテーション・座学

**内容**
- OTAとは何か、なぜ車載ソフトウェアでOTAが使われるかの説明
- Dual-bank Flashの仕組み（Bank A=稼働中、Bank B=更新用）の説明
- デジタル署名によるデータ完全性の説明（AWS Signerの役割）

**所要時間**: シナリオA/B共通 **30分**

---

### 第1章: AWS IoT Core確認・設定

**シナリオA（事前準備済み）の場合**
1. インストラクターが用意したThing／証明書／ポリシーの中身をAWSマネジメントコンソールで一緒に確認
2. MQTTテストクライアントで、デバイスがpublishしているトピックを購読し、動作を確認

**所要時間（シナリオA）**: 20〜30分

**シナリオB（ゼロから）の場合**
1. AWS IoT Coreコンソールで「モノ（Thing）」を新規作成
2. デバイス証明書を発行し、Thingにアタッチ
3. ポリシーを作成し証明書にアタッチ（publish/subscribe許可対象トピックの設定を含む）
4. 証明書一式（デバイス証明書・秘密鍵・ルートCA）をデバイス側の設定ファイルに反映
5. MQTTテストクライアントで疎通確認

**所要時間（シナリオB）**: 60〜90分（新卒がAWSコンソール操作に不慣れな場合、証明書のダウンロード忘れやポリシー設定ミスで詰まりやすい）

---

### 第2章: 正常系ハンズオン（Bank B自動起動）

**内容**
1. バージョン番号を上げたファームウェア（v2）をビルド
2. AWS Signerでファームウェアに署名（署名プロファイルは事前作成済み想定）
3. 署名済みファームウェアをS3バケットへアップロード
4. AWS IoT CoreでOTA Update Jobを作成し、対象Thingへ配信
5. デバイス側でOTA Agentが自動的に：
   - MQTT経由でジョブ通知を受信
   - S3からファームウェアをダウンロード
   - 署名検証（公開鍵は書き込み済みのため自動で通る）
   - Bank Bへ書き込み
   - 再起動してBank Bで起動、正常起動を確認したらそのまま稼働継続
6. シリアルコンソール／IoT Coreのジョブステータスで成功を確認

**所要時間**: シナリオA/B共通で **45〜60分**（ビルド・署名・アップロード・ジョブ作成の操作に加え、実機のダウンロード〜再起動〜起動確認の待ち時間を含む）

**補足**: ここは新卒にとって初めての「クラウド→実機」の一連の流れを追う体験。操作自体は手順通りだが、初見では「今どのステップで待っているのか」が分かりにくく、質問対応の時間を見込んでおく。

---

### 第3章: 異常系ハンズオン（自動フェイルオーバー）

**内容**
1. 起動直後に無限ループに入るだけの「わざと壊れたファームウェア（v3）」を用意（事前に用意したコードを使う想定。学生に書かせる場合は数十行程度の改変体験として組み込む）
2. 正常系と同じ手順で署名→S3アップロード→OTA Update Job作成
3. デバイスがv3をダウンロード・検証（署名自体は正しいので検証はパスする点に注意）→ Bank Bへ書き込み→再起動
4. 起動後、無限ループによりウォッチドッグ等で異常検知 → ブートローダーが自動的にBank A（旧ファームウェア）へロールバック
5. IoT Core側でジョブが失敗ステータスになることを確認

**所要時間**: **30〜45分**

**補足**: 「署名は正しいのに中身が壊れている」ケースであることを明確に説明し、次章の「署名自体が不正なケース」と区別させる。

---

### 第4章（任意）: 署名検証NGケース

**内容**
1. 署名なし、または改ざんされたファームウェアバイナリを用意
2. 同様にOTA Update Jobとして配信
3. デバイス側のOTA Agentが署名検証の時点でエラーとなり、ダウンロード自体は完了してもアップデートが実行されないことを確認

**所要時間**: **20〜30分**（時間が押している場合は省略可、または座学内での説明のみに縮小可）

---

### 第5章: 振り返り・質疑応答

**内容**: 学んだこと（フェイルオーバー・署名検証）の振り返り、質疑応答

**所要時間**: 30分

---

## 3. 合計時間の見積もりサマリー

| 章 | シナリオA（事前準備済み） | シナリオB（ゼロから） |
|---|---|---|
| 第0章 座学 | 30分 | 30分 |
| 第1章 IoT Core確認/設定 | 20〜30分 | 60〜90分 |
| 第2章 正常系 | 45〜60分 | 45〜60分 |
| 第3章 異常系（フェイルオーバー） | 30〜45分 | 30〜45分 |
| 第4章 署名検証NG（任意） | 20〜30分 | 20〜30分 |
| 第5章 振り返り | 30分 | 30分 |
| **合計** | **約3時間〜4時間15分** | **約3時間35分〜5時間15分** |

第4章を省略する場合はどちらのシナリオも20〜30分短縮される。

**結論**: 提案資料9枚目の「3時間」という見積もりは、シナリオA（事前準備を手厚くする）であれば概ね妥当だが、余裕を持たせるなら**半日（4時間程度）**を基本線とし、新卒が初めてAWSコンソールを触る場合や第4章まで含める場合は**半日強〜1日がかり**を見込んでおくのが安全。

---

## 4. つまずきやすいポイント・リスク

- **AWSコンソール操作の不慣れ**: Thing/証明書/ポリシーの関連付けを間違える、証明書ダウンロードを忘れる、といったミスで第1章が長引きやすい
- **Wi-Fi/ネットワーク設定**: RX65Nボードのネットワーク接続設定（SSID/パスワード投入）で個別トラブルが起きやすく、1人あたり数分の差が全体の遅延に積み上がる
- **書き込みツールの扱い**: Renesas Flash ProgrammerやE2 Studioでの書き込み手順に不慣れだと、初回書き込みで時間を取られる
- **「待ち時間」の体感**: OTAのダウンロード〜検証〜再起動には数十秒〜数分の待ち時間が発生し、初見の新卒には「フリーズしているのでは」という不安が生まれやすいので、進行担当が状況を都度説明するとよい
- **人数規模による並列度**: 1人1台のボードを用意できるか、グループで1台を共有するかで、実質的な体験時間（順番待ち）が変わる

---

## 5. 提案資料へのフィードバック（任意）

`proposal_slides.md` のスライド9（実施スケジュール）は「半日（3時間）」を前提として書かれているが、上記の見積もりに基づくと、新卒が初めてAWSコンソール操作を行うシナリオBに近い場合や第4章まで実施する場合は**半日を超える可能性**がある。反映するかどうかはご判断のうえ、必要であればスライド9の時間割・所要時間表記を更新する。
