# 「ミニOTAハンズオン演習」本番セッション 作業洗い出し・所要時間見積もり

対象マイコン: Renesas RX65N系（CK-RX65N / Renesas Starter Kit+ for RX65N-2MB を想定）

**方針確定**: シナリオB（新卒がゼロからAWS環境構築・ボードセットアップを行う）を採用。インストラクターが事前に用意するのは**ファームウェアバイナリのみ**（初期書き込み用イメージ、正常系OTA用イメージ、異常系OTA用イメージ、署名検証NG確認用の未署名/改ざんイメージ）。その他（AWSリソース一式・ボードへの書き込み作業・Wi-Fi設定）はすべて新卒本人が当日行う。許容時間は**6時間程度**。

---

## 0. まず訂正しておきたい前提

企画段階では「異常系のフェイルオーバーを実現するには、公開鍵の検証ロジックを自分たちで実装する必要があるはず」という想定だったが、調査の結果これは誤り。

RX65N向けにAWSとRenesasが公式に提供している参照実装（[AWS公式チュートリアル](https://docs.aws.amazon.com/freertos/latest/userguide/download-rx65n-ota.html)、現行の [`renesas/iot-reference-rx`](https://github.com/renesas/iot-reference-rx)）には、以下がすべて**ミドルウェアとして実装済み**で提供されている。

| 機能 | 実装状況 | 必要な作業 |
|---|---|---|
| 署名検証（ECDSA/PKCS#11） | 提供済み | `code_signer_public_key.h` に公開鍵を貼り付けるだけ（コード実装は不要、設定のみ） |
| デュアルバンク切り替え | 提供済み | `BSP_CFG_CODE_FLASH_BANK_MODE` 等の設定のみ |
| 起動失敗時の自動ロールバック | 提供済み | `BL_ENABLE_ROLLBACK=1` を設定するだけ |

つまり「正常系」「異常系（フェイルオーバー）」「署名検証」はいずれも**実装ではなく設定・確認作業**であり、暗号ロジックやブートローダーを自作する必要はない。

唯一の"実装"らしい作業（異常系デモ用にわざと壊れたファームウェアを用意すること）は、今回**インストラクター側の事前準備**に含めるため、新卒の当日作業には含まれない。

新卒の当日作業で時間がかかるのは、実装ではなく「AWSリソースをゼロから構築する操作」「マイコンの書き込み・Wi-Fi設定」「初めて触る操作でのつまずき」である。

---

## 1. 準備物の切り分け

| 項目 | 誰が準備するか |
|---|---|
| 初期書き込み用ファームウェア（aws_demos + bootloader 結合済みイメージ） | インストラクター（事前） |
| 正常系OTA用ファームウェア（v2） | インストラクター（事前） |
| 異常系OTA用ファームウェア（起動直後に無限ループ等、v3） | インストラクター（事前） |
| 署名検証NG確認用ファームウェア（未署名 or 改ざん版） | インストラクター（事前） |
| AWSアカウント・IAMユーザー | インストラクター（事前、各自ログイン情報のみ配布） |
| IAMロール（OTA更新用サービスロール） | 新卒（当日） |
| S3バケット | 新卒（当日） |
| AWS IoT Core（Thing／証明書／ポリシー） | 新卒（当日） |
| AWS Signer（鍵ペア生成・証明書・署名プロファイル） | 新卒（当日） |
| ボードへの初回書き込み・Wi-Fi設定 | 新卒（当日） |

---

## 2. 章立てと手順（手順書レベル、6時間枠）

### 第0章: オリエンテーション・座学

**内容**
- OTAとは何か、なぜ車載ソフトウェアでOTAが使われるかの説明
- Dual-bank Flashの仕組み（Bank A=稼働中、Bank B=更新用）の説明
- デジタル署名によるデータ完全性の説明（AWS Signerの役割）
- 当日の作業の全体像（この後、自分たちでAWS環境を一から作ることの説明）

**所要時間**: 30分

---

### 第1章: AWS環境構築（ゼロから）

**内容**
1. **IAMロール作成**: OTA Updateジョブが使用するサービスロール（S3読み取り・IoT関連権限）を作成
2. **S3バケット作成**: ファームウェアバイナリ格納用のバケットを新規作成
3. **AWS IoT Core設定**
   - 「モノ（Thing）」を新規作成
   - デバイス証明書を発行しThingにアタッチ
   - ポリシーを作成し証明書にアタッチ（publish/subscribe許可トピックの設定を含む）
4. **AWS Signer設定**
   - OpenSSLでECDSA鍵ペアを生成
   - 自己署名証明書を作成し、AWS Certificate Manager（プライベート証明書）へインポート
   - AWS Signerで署名プロファイル（Signing Profile）を作成し、上記証明書と紐付け
5. 証明書一式（デバイス証明書・秘密鍵・ルートCA、公開鍵）を後工程で使うため整理しておく

**所要時間**: **90〜120分**（新卒がAWSコンソール・OpenSSL CLI操作に不慣れな場合、証明書のインポート順序ミスやポリシー設定ミスで詰まりやすい。手順書は画面キャプチャ付きで用意し、質問対応の人員を厚めに配置する）

**休憩**: 10分

---

#### 第1章-3 詳細手順書: AWS IoT Core設定（Thing・証明書・ポリシー・MQTT疎通確認）

上記「3. AWS IoT Core設定」を、受講生が実際に画面を操作するレベルまで分解した手順。AWSマネジメントコンソールの構成は将来的に変わる可能性があるため、実施前に最新画面との差分を確認すること。

**1.3.1 AWS IoT Coreコンソールを開く**
1. AWSマネジメントコンソールにログイン（配布されたIAMユーザー情報を使用）
2. 検索バーで「IoT Core」と入力し、サービス一覧から「IoT Core」を選択

**1.3.2 モノ（Thing）を作成する**
1. 左メニュー「管理」→「すべてのデバイス」→「モノ」を開く
2. 「モノを作成」ボタンをクリック
3. 「単一のモノを作成」を選択して「次へ」
4. 「モノの名前」を入力（例: `ck-rx65n-<自分の名前のローマ字>`。他の受講生と重複しない名前にする）
5. 「モノのタイプ」「検索可能な属性」は今回は設定不要、そのまま「次へ」

> **つまずきポイント**: モノの名前は後でMQTTトピック名やデバイス側の設定にもそのまま使うことが多い。ここで適当な名前を付けると後工程で混乱するので、命名規則を最初に揃えておく。

**1.3.3 デバイス証明書を発行する**
1. 「デバイス証明書」の画面で「1-Click証明書作成（推奨）」を選択し「次へ」
2. 証明書が自動生成される
3. 以下4つのファイルを**すべて**ダウンロードする
   - デバイス証明書（`xxxxx-certificate.pem.crt`）
   - パブリックキー（`xxxxx-public.pem.key`）
   - プライベートキー（`xxxxx-private.pem.key`）
   - Amazon Root CA 1（ルートCA証明書、ページ内リンクからダウンロード）
4. ダウンロードしたファイルは、あとで迷わないよう専用フォルダ（例: `C:\ota-handson\<自分の名前>\certs\`）にまとめて保存する

> **つまずきポイント（最重要）**: プライベートキーはこの画面でしかダウンロードできない。ダウンロードし忘れて画面を閉じると、証明書を作り直すことになる。4ファイルすべて揃っているか、この時点で必ず確認する。

**1.3.4 証明書を有効化する**
1. 証明書作成直後の画面、または「セキュリティ」→「証明書」の一覧から対象証明書を開く
2. ステータスが「非アクティブ」になっている場合は「アクティブ化」を選択してステータスを「アクティブ」に変更

> **つまずきポイント**: 証明書を有効化し忘れると、デバイス側は正しい証明書を使っていてもMQTT接続が拒否される。「証明書を作ったのに繋がらない」の原因の大半はこれ。

**1.3.5 ポリシーを作成する**
1. 左メニュー「セキュリティ」→「ポリシー」→「ポリシーを作成」
2. ポリシー名を入力（例: `ck-rx65n-policy-<自分の名前>`）
3. ポリシードキュメントに以下のようなJSONを入力する（`<AWSアカウントID>`と`<リージョン>`は配布情報に置き換え、モノの名前も自分のものに置き換える）

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": "iot:Connect",
      "Resource": "arn:aws:iot:<リージョン>:<AWSアカウントID>:client/ck-rx65n-<自分の名前>"
    },
    {
      "Effect": "Allow",
      "Action": ["iot:Publish", "iot:Receive"],
      "Resource": "arn:aws:iot:<リージョン>:<AWSアカウントID>:topic/ck-rx65n-<自分の名前>/*"
    },
    {
      "Effect": "Allow",
      "Action": "iot:Subscribe",
      "Resource": "arn:aws:iot:<リージョン>:<AWSアカウントID>:topicfilter/ck-rx65n-<自分の名前>/*"
    }
  ]
}
```

4. 「作成」をクリック

> **つまずきポイント**: リソースARNのモノ名・トピック名のスペルミスで権限エラーになりやすい。第3〜5章で使うトピック名（正常系/異常系のジョブ通知トピック等）を先に決めてから、それに合わせてポリシーのResourceを書くこと。

**1.3.6 ポリシーを証明書にアタッチする**
1. 「セキュリティ」→「証明書」から対象の証明書を開く
2. 「ポリシー」タブ（または証明書詳細画面のポリシーアタッチメニュー）から「ポリシーをアタッチ」を選択
3. 1.3.5で作成したポリシーを選択してアタッチ
4. 同じ証明書詳細画面から、1.3.2で作成した「モノ」にも証明書がアタッチされていることを確認（未アタッチであれば「モノをアタッチ」で関連付ける）

> **つまずきポイント**: 「証明書にポリシーをアタッチ」と「証明書にモノをアタッチ」は別操作。どちらか一方だけやって「繋がらない」と詰まるケースが多いので、この時点で両方揃っているか指差し確認させる。

**1.3.7 ここまでの状態を確認する**
- モノ（Thing）1つ、証明書1つ、ポリシー1つが作成されている
- 証明書はアクティブ化されている
- 証明書には「モノ」と「ポリシー」の両方がアタッチされている
- ローカルに4つの証明書ファイル（デバイス証明書・公開鍵・秘密鍵・ルートCA）が保存されている

**1.3.8 MQTTテストクライアントで疎通確認する**
1. 左メニュー「テスト」→「MQTTテストクライアント」を開く
2. 「トピックへの購読」タブで、1.3.5のポリシーで許可したトピック（例: `ck-rx65n-<自分の名前>/#`）を入力し「購読」をクリック
3. 別タブまたは同画面の「トピックへ発行」で、同じトピック配下（例: `ck-rx65n-<自分の名前>/test`）にテストメッセージ（例: `{"message": "hello"}`）を発行
4. 「トピックへの購読」画面にメッセージが表示されれば、AWS IoT Core側の設定（Thing・証明書・ポリシー）は正しく機能している

> **補足**: この時点ではまだ実機（CK-RX65N）は関与しない。コンソール上のテストクライアント同士でメッセージが届くことを確認するだけで、「AWS側の設定ミスなのか」「後工程の実機側の設定ミスなのか」を切り分けられる。この切り分けができていると、第2章でボードが繋がらないときの原因調査が格段に速くなる。

**この詳細手順の所要時間目安**: 40〜60分（第1章全体の90〜120分のうち、IoT Core部分の内訳）

---

### 第2章: ボード初期セットアップ

**内容**
1. インストラクターから配布された初期書き込み用イメージ（結合済み.mot）を、Renesas Flash Programmer等でボードに書き込み
2. Wi-Fi設定（SSID/パスワード）をデバイス側の設定に反映（設定ファイル書き換え＋再書き込み、またはコンソール入力）
3. 発行済み証明書一式・公開鍵をデバイス側の該当ファイルへ反映し、再書き込み
4. シリアルコンソールで起動ログを確認し、AWS IoT Coreへの接続（MQTT接続）が確立していることを確認

**所要時間**: **35〜45分**（証明書反映のための再書き込みが発生するため、単純な初回書き込みより時間がかかる点に注意）

---

#### 第2章 詳細手順書: ボード初期セットアップ

受講生は初期書き込み用の結合済みイメージ（`.mot`ファイル）のみを受け取る想定（プロジェクトのビルド環境は不要）。証明書やWi-Fi情報はビルド時に埋め込むのではなく、**起動後にシリアルターミナルからCLIで投入する**方式（`iot-reference-rx`のプロビジョニングCLI）を前提とする。

**2.1 ボードとPCを接続する**
1. CK-RX65NのUSBコネクタ「J14」とPCをUSBケーブルで接続する（電源供給・書き込み用）
2. Ethernetケーブルでボードをネットワークに接続する（インターネット到達可能な環境であること）
3. ジャンパ設定を確認する: J2短絡／J11開放／J16は1-2短絡（ボードによって出荷時設定と異なる場合があるため必ず確認）

**2.2 初期イメージを書き込む**
1. Renesas Flash Programmer（またはインストラクター指定の書き込みツール）を起動
2. 新規プロジェクト作成でマイコン「RX65N」、通信方式「USB」を選択し、ボードと接続
3. 配布された結合済みイメージファイル（`xxxx.mot`）を「参照」から指定
4. 「スタート」で書き込みを実行し、完了メッセージを確認

> **つまずきポイント**: 書き込みツールがボードを認識しない場合、USBドライバ未インストール、またはJ14ではなくJ10に接続してしまっているケースが多い。書き込み用と後述のシリアル通信用でUSBコネクタが異なる点に注意。

**2.3 シリアルターミナルで接続する**
1. Tera Termを起動し、書き込みとは別のUSBシリアルポート（J10経由でPCに認識されるCOMポート）を選択して接続
2. ボーレート等のシリアル設定はインストラクターの配布資料の値に合わせる
3. ボードをリセット（リセットボタン押下、または電源再投入）し、起動ログが流れることを確認

**2.4 プロビジョニングCLIモードに入る**
1. 起動ログが流れた直後、以下を入力してCLIモードに切り替える
   ```
   CLI
   ```
2. データフラッシュを初期化する（初回のみ）
   ```
   format
   ```

> **つまずきポイント**: CLIモードへの切り替えは起動後の一定時間内のみ受け付けられる（タイムアウトあり）。ログが流れ終わる前後を逃すと再度リセットからやり直しになる。

**2.5 Thing名・接続先エンドポイントを設定する**
1. 第1章-3で作成した「モノ（Thing）」の名前を設定する
   ```
   conf set thingname <1.3.2で作成したThing名>
   ```
2. AWS IoT CoreのエンドポイントURLを設定する（AWS IoT Coreコンソール「設定」画面で確認できる、自分のアカウント固有の値）
   ```
   conf set endpoint <自分のAWSアカウントのIoTエンドポイント>
   ```

**2.6 証明書・秘密鍵を設定する**
1. 証明書設定コマンドを入力する
   ```
   conf set cert
   ```
2. Tera Termの画面に、第1章-3でダウンロードした**デバイス証明書ファイル**をドラッグ＆ドロップする
3. 表示される送信方法の選択で「Send File (Paste content of file)」を選び、送信を確定する
4. 同様に秘密鍵を設定する
   ```
   conf set key
   ```
5. **プライベートキーファイル**をドラッグ＆ドロップし、同様に送信する

> **つまずきポイント**: ここで公開鍵ファイルや証明書ファイルを取り違えると、あとでMQTT接続時にTLSハンドシェイクエラーとなる。ファイル名（`-certificate.pem.crt` / `-private.pem.key`）を必ず確認してからドラッグ＆ドロップすること。

**2.7 Wi-Fi設定を投入する**（該当ボード構成の場合）
- CK-RX65NがWi-Fiモジュール（DA16600等）経由で接続する構成の場合、SSID/パスワードの設定コマンドが別途存在する。**この手順書時点ではコマンドの正確な仕様が未確認のため、実施前に配布資料の「Wi-Fiネットワーク設定」該当ページで正確なコマンド文字列を確認すること**（Ethernet接続構成の場合はこの手順は不要）

**2.8 設定を確定して再起動する**
```
conf commit
reset
```

**2.9 起動確認**
1. リセット後、起動ログをシリアルターミナルで確認する
2. AWS IoT CoreへのMQTT接続が確立したことを示すログ（接続成功メッセージ）が表示されることを確認
3. AWS IoT Coreコンソール「モニタリング」または「MQTTテストクライアント」側からも、対象Thingがオンラインになっていることを確認する

> **ここまでの状態を確認する（チェックリスト）**
> - 初期イメージの書き込みが完了している
> - シリアルターミナルで起動ログが正常に表示される
> - `conf commit`後にリセットしてもエラーなく起動する
> - AWS IoT Core側でデバイスの接続（オンライン状態）が確認できる

**この詳細手順の所要時間目安**: 35〜45分（Wi-Fi設定確認の手戻りが発生した場合はさらに時間を要する可能性あり）

---

### 第3章: 正常系ハンズオン（Bank B自動起動）

**内容**
1. インストラクターから配布された正常系ファームウェア（v2）を、AWS Signerで署名
2. 署名済みファームウェアをS3バケットへアップロード
3. AWS IoT CoreでOTA Update Jobを作成し、対象Thingへ配信
4. デバイス側でOTA Agentが自動的に：
   - MQTT経由でジョブ通知を受信
   - S3からファームウェアをダウンロード
   - 署名検証（自分で設定した公開鍵で自動検証）
   - Bank Bへ書き込み
   - 再起動してBank Bで起動、正常起動を確認したらそのまま稼働継続
5. シリアルコンソール／IoT Coreのジョブステータスで成功を確認

**所要時間**: **45〜60分**

**補足**: 「クラウド→実機」の一連の流れを追う最初の体験。操作自体は手順通りだが、初見では「今どのステップで待っているのか」が分かりにくく、質問対応の時間を見込んでおく。

**休憩**: 10分

---

#### 第3章 詳細手順書: 正常系ハンズオン（Bank B自動起動）

前提: 第1章-4でAWS Signerの署名プロファイル・証明書が作成済み、第2章でデバイスがAWS IoT Coreにオンライン接続済みであること。

**3.1 配布ファイルを確認する**
1. インストラクターから配布された正常系ファームウェアバイナリ（例: `app_v2.bin`）を、指定の作業フォルダに配置する
2. ファイル名にバージョン番号が含まれているか確認する（v1=初期書き込み済み、v2=今回配信するもの、という対応関係を見失わないため）

**3.2 OTA Update Jobを作成する**
1. AWS IoT Coreコンソールを開き、左メニュー「管理」→「リモートアクション」→「ジョブ」（コンソールのバージョンによっては「ソフトウェア更新」等の名称）を開く
2. 「ジョブを作成」→「FreeRTOS OTAアップデートジョブを作成」を選択
3. 「ジョブ名」を入力（例: `ota-normal-<自分の名前>`）、説明は任意
4. 「次へ」

**3.3 対象デバイスを選択する**
1. 「デバイスを選択」で、第1章-3・第2章で作成・接続した自分のThingを選択
2. 「次へ」

**3.4 ファイルの署名を設定する**
1. 「ファイルの署名と選択」画面で「新しいファイルに署名する」を選択
2. 「ファイルをアップロード」で、3.1で配置した未署名の`app_v2.bin`を指定し、第1章-2で作成したS3バケットへアップロード
3. 「コード署名プロファイル」で、第1章-4で作成した署名プロファイルを選択（未作成の場合はこの画面で新規作成も可能）
4. 「デバイス上の証明書のパス名」等、デバイス側のOTA PAL設定と一致させる必要がある項目は、配布資料に記載の既定値をそのまま入力する（この値はプロジェクトのソースコード側の設定と対応しているため、勝手に変更しないこと）
5. 「IAMロール」で、第1章-1で作成したOTA用サービスロールを選択

> **つまずきポイント**: 「デバイス上の証明書のパス名」を誤ると、署名検証には成功してもデバイス側で公開鍵を見つけられずOTAが失敗する。値は事前配布資料の指定値を必ずそのまま使うこと。

**3.5 ジョブを作成する**
1. 内容を確認し「ジョブを作成」をクリック
2. ジョブの一覧画面に遷移し、ステータスが「IN_PROGRESS」になっていることを確認

**3.6 デバイス側の動作を観察する**
1. シリアルターミナル（Tera Term）のログを見ながら、以下が順番に発生することを確認する
   - MQTT経由でのジョブ通知受信ログ
   - S3からのファームウェアダウンロード進捗ログ
   - 署名検証成功のログ
   - Bank Bへの書き込み完了ログ
   - 再起動ログ
2. 再起動後、新しいファームウェア（v2）で正常に起動し、MQTT再接続していることをログで確認する

**3.7 成功を確認する**
1. AWS IoT Coreコンソールのジョブ詳細画面に戻り、ステータスが「SUCCEEDED」になっていることを確認
2. デバイスがBank Bで起動し、そのまま安定稼働していることを、シリアルログのバージョン表示（起動時にファームウェアバージョンを出力するログがある場合）で確認する

> **ここまでの状態を確認する（チェックリスト）**
> - AWS IoT Coreのジョブステータスが「SUCCEEDED」
> - シリアルログでダウンロード〜検証〜書き込み〜再起動の一連の流れがすべて確認できた
> - 再起動後もMQTT接続が維持されている（切断されたままになっていない）

**この詳細手順の所要時間目安**: 45〜60分（うちジョブ作成操作自体は10〜15分程度、残りはダウンロード・検証・再起動の待ち時間と観察）

---

### 第4章: 異常系ハンズオン（自動フェイルオーバー）

**内容**
1. インストラクターから配布された異常系ファームウェア（起動直後に無限ループへ入るv3）を、第3章と同じ手順で署名→S3アップロード→OTA Update Job作成
2. デバイスがv3をダウンロード・検証（署名自体は正しいので検証はパスする点に注意）→ Bank Bへ書き込み→再起動
3. 起動後、無限ループによりウォッチドッグ等で異常検知 → ブートローダーが自動的にBank A（旧ファームウェア）へロールバック
4. IoT Core側でジョブが失敗ステータスになることを確認

**所要時間**: **35〜45分**

**補足**: 「署名は正しいのに中身が壊れている」ケースであることを明確に説明し、次章の「署名自体が不正なケース」と区別させる。

---

### 第5章: 署名検証NGケース

**内容**
1. インストラクターから配布された未署名/改ざん済みファームウェアを、OTA Update Jobとして配信
2. デバイス側のOTA Agentが署名検証の時点でエラーとなり、ダウンロード自体は完了してもアップデートが実行されないことを確認

**所要時間**: **20〜30分**

---

### 第6章: 振り返り・質疑応答

**内容**: 学んだこと（環境構築の体験・フェイルオーバー・署名検証）の振り返り、質疑応答

**所要時間**: 30分

---

## 3. 合計時間の見積もりサマリー

| 章 | 所要時間 |
|---|---|
| 第0章 座学 | 30分 |
| 第1章 AWS環境構築（ゼロから） | 90〜120分 |
| 休憩 | 10分 |
| 第2章 ボード初期セットアップ | 35〜45分 |
| 第3章 正常系 | 45〜60分 |
| 休憩 | 10分 |
| 第4章 異常系（フェイルオーバー） | 35〜45分 |
| 第5章 署名検証NG | 20〜30分 |
| 第6章 振り返り | 30分 |
| **合計** | **約5時間25分〜6時間50分** |

許容時間6時間に対して、標準ケースで概ね収まるが、つまずきが重なる場合（特に第1章のAWS環境構築）は6時間を超過する可能性がある。想定進行は以下の通り：

- **標準進行（つまずき少なめ）**: 約5時間25分 → 6時間枠内に十分収まり、質疑応答に余裕を持たせられる
- **やや遅延進行（新卒の多くが初めてAWSコンソール・OpenSSLに触れる）**: 約6時間50分 → 6時間を若干超過するため、以下いずれかの調整を推奨
  - 第5章（署名検証NGケース）を座学内の説明のみに簡略化し20〜30分短縮
  - 第1章のAWS Signer設定（鍵ペア生成・証明書インポート）部分を、コマンドをコピペで実行できるスクリプト化した手順書にして時間短縮を図る

---

## 4. つまずきやすいポイント・リスク

- **AWS Signerまわりの複雑さ**: OpenSSLでの鍵ペア生成、ACMへの証明書インポート、Signing Profileの作成は手順が多段階でつまずきやすい。今回のスコープで最も時間振れ幅が大きい箇所
- **IAMロール・ポリシーの設定ミス**: OTA Update Jobに必要な権限（S3読み取り、IoT関連）が不足すると、後工程（第3章）でジョブが失敗し、原因切り分けに時間がかかる
- **証明書の反映漏れ**: デバイス側の設定ファイルに証明書・公開鍵を正しく反映できておらず、MQTT接続やOTA検証で失敗するケースが起きやすい
- **Wi-Fi/ネットワーク設定**: RX65Nボードのネットワーク接続設定で個別トラブルが起きやすく、1人あたり数分の差が全体の遅延に積み上がる
- **「待ち時間」の体感**: OTAのダウンロード〜検証〜再起動には数十秒〜数分の待ち時間が発生し、初見の新卒には不安が生まれやすいので、進行担当が状況を都度説明するとよい
- **人数規模による並列度**: 1人1台のボードを用意できるか、グループで1台を共有するかで、実質的な体験時間（順番待ち）が変わる
- **サポート体制**: 全工程を新卒に任せるため、特に第1章はつまずきが集中しやすく、質問対応要員（TA的な立ち位置）を複数名配置することを推奨

---

## 5. 提案資料へのフィードバック（反映推奨）

`proposal_slides.md` のスライド9（実施スケジュール）は「半日（3時間）」を前提としていたが、シナリオB（新卒がAWS環境構築から行う）を採用する場合は**6時間程度（1日がかり）**が実態に近い。スライド9の時間割・所要時間表記を6時間ベースに更新することを推奨する（別途対応）。
