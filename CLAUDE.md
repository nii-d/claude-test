# CLAUDE.md

このファイルは、このリポジトリで作業する Claude Code (claude.ai/code) へのガイダンスを提供します。

---

## 基本ルール

- **回答・コメント・コミットメッセージはすべて日本語で記述する**
- 簡潔に答える。不要な前置きや要約の繰り返しはしない
- 実装前に方針を一言確認する（大きな変更・設計判断が必要な場合）
- ファイルを参照するときはパスと行番号をセットで示す（例: `src/main.py:42`）

---

## 禁止事項

以下の操作は**ユーザーの明示的な指示がない限り絶対に行わない**。

| 禁止操作 | 理由 |
|---|---|
| `git push --force` / `git reset --hard` | 履歴の破壊・他者の作業の消失 |
| `rm -rf` による広範囲削除 | 復元不能なファイル消失 |
| 本番環境への直接デプロイ | 意図しないサービス停止・データ破損 |
| 環境変数・シークレットのハードコード | 認証情報の漏洩 |
| `--no-verify` でのコミット | フックによる品質チェックの回避 |
| 共有ブランチ（main/master）への直接コミット | レビューなしの変更混入 |
| 外部サービスへのデータ送信（URLの生成・投稿含む） | 意図しない情報公開 |

不可逆・影響範囲が広い操作は、実行前に必ず内容を説明してユーザーの確認を取る。

---

## コードスタイル

- コメントは「なぜ」が非自明な箇所にのみ書く（何をしているかは書かない）
- 不要な抽象化・将来のための設計はしない。今必要なものだけ実装する
- エラーハンドリングはシステム境界（ユーザー入力・外部 API）にのみ追加する

---

## Git ルール

- コミットはユーザーに明示的に依頼されたときだけ作成する
- コミットメッセージは日本語で、変更の「なぜ」を書く
- PR・Issue への投稿はユーザーの確認後に行う

---

## このリポジトリについて

Claude Code の動作検証・実験用リポジトリ。本番コードは含まない。
